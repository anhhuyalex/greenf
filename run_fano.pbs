#PBS -S /bin/bash
#PBS -N localization5
#PBS -l nodes=1:ppn=2

export MKL_NUM_THREADS=2
export OMP_NUM_THREADS=2
echo "PBS_NODEFILE" $PBS_NODEFILE
cat $PBS_NODEFILE

# to set up the upper memory limit for an array 
ulimit -s unlimited
 
echo "Remove trash"
cd /tmp
#rm -rf *
echo "Starting run at: `date`"
cd $PBS_O_WORKDIR

#if [ -d "/tmp/alexn1000" ]; then
#	rm -r /tmp/alexn1000/
#fi

# mkdir -p /tmp/alexn1000
#cp test.py /tmp/alexn1000/.
cp localizationw\=1.py /tmp/alexn1000/.
cp localizationw\=2.py /tmp/alexn1000/.
cp localizationw\=3.py /tmp/alexn1000/.
cp localizationw\=4.py /tmp/alexn1000/.
cp localizationw\=5.py /tmp/alexn1000/.
# cp -r ../Data /tmp/alexn1000/.
cd /tmp/alexn1000
# mkdir -p greenf

# mkdir Results_temp

echo "Working directory is" $PBS_O_WORKDIR
echo "Running directory is" $PWD
# TEST
#(time python test.py > out.txt) >& expand_time.out

which python
#python localizationw\=1.py 
#python localizationw\=2.py 
#python localizationw\=3.py 
#python localizationw\=4.py 
python localizationw\=5.py 
#python test.py

echo "The job has been completed."
echo "compress the output"
cd $PBS_O_WORKDIR 
#echo "Move the output to the working directory"
echo "Move the Folder Results and out.txt"
ls /tmp/alexn1000/
cp -r /tmp/alexn1000/* /home/anguyen/diagonalization/localize
cd /tmp
echo "Remove temporary files"
# rm -rf alexn1000
echo "Program finished at: `date`"

